{
   "regions": ["gru1"],
  "framework": "nextjs",
  "buildCommand": "prisma generate && next build",
  "installCommand": "npm install",
  "env": {
    "DATABASE_URL": "@database-url",
    "DIRECT_URL": "@direct-url"
  },
  "crons": [
    {
      "path": "/api/cron/trial-vencido",
      "schedule": "0 12 * * *"
    }
  ]
}