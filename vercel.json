{
  "regions": ["gru1"],
  "framework": "nextjs",
  "buildCommand": "prisma generate && next build",
  "installCommand": "npm install",
  "crons": [
    {
      "path": "/api/cron/trial-vencido",
      "schedule": "0 12 * * *"
    }
  ]
}